# GroupFinder Backend

This is the Express backend for GroupFinder.

Check env.example for an example .env file

To run this:

    npm run build
    npm start

or just:

    npm run dev

To run the tests:

    npm run test
